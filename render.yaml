services:
  - type: web
    name: seatlock-api
    runtime: node
    plan: free
    buildCommand: npm ci --include=dev && npm run build -w @seatlock/api
    startCommand: npm run start:production -w @seatlock/api
    healthCheckPath: /health
    envVars:
      - key: NODE_ENV
        value: production
      - key: DATABASE_URL
        sync: false
      - key: JWT_SECRET
        generateValue: true
      - key: ALLOWED_ORIGINS
        sync: false
